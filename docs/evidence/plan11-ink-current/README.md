# Plan 11 ink — CURRENT head evidence (review round 2, P1-1)

Fresh captures of the actual PR head (`6cbc705`: ink identity + the
owner's modern-grammar prototype + the in-flow action-pill fix).
Supersedes `plan11-ink/` as the review artifact — that older set was
captured at `b807ec0`, BEFORE the prototype, and no longer represents
the head (it still shows the white topbar, emoji bell, callout task
cards, and banner hero). Kept for comparison history per the review.

Matrix: same §17.0 contract — Chromium, desktop 1440×900 + mobile
375×812, seeded Plan 10 world per pass, wrong-artifact guard active.

- desktop: Dashboard, Task Square, Task Detail, Claim Detail,
  Teacher review queue, Admin users
- mobile: Dashboard, Claim Detail (the dense detail page)

The next artifact after this set: the three-screen signature-motif
spike (Dashboard / Task Square / Claim Detail) from the same review —
segment-rail quest motif, context glyphs, achievement stat band,
brand mark, rail account footer, frosted mobile nav.
